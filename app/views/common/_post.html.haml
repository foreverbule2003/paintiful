.col-lg-3.col-md-4.col-xs-12.post
  .thumbnail#index
    =link_to (image_tag post.image.url(:small)), post
    %hr/

    %span.badge
      =post.counter 
      %i.fa.fa-eye 
      
    %span.badge
      =post.like
      %i.fa.fa-heart

    = link_to like_post_path(post), method: :get, class: "btn btn-danger btn btn-sm pull-right" do 
      %i.fa.fa-heart 