.container-fluid#index
  -@category.posts.each do |post|
    #post.col-md-3
      =link_to (image_tag post.image.url(:small), class: "thumbnail"), post, class: "thumbnail_wrap"
      %i.fa.fa-eye 
        =post.counter      
      %i.fa.fa-heart
        =post.like
      = link_to like_post_path(post), method: :get, class: "btn btn-danger pull-right" do 
        %i.fa.fa-heart 
      